{
  "static": {
    "_comment": "These fields are set once. Edit manually. Never overwritten by the transformer.",
    "repo_name": "{{PROJECT_NAME}}",
    "primary_language": "{{LANGUAGE_1}}",
    "language_version": "{{BUILD_SYSTEM}}",
    "frameworks": [
      "{{FRAMEWORK_1}}"
    ],
    "entry_point": "{{ENTRY_POINT_1}}",
    "api_protocol": "REST",
    "restricted_write_paths": [
      "config/",
      "build/"
    ],
    "environment_variables": {
      "{{ENV_VAR_1}}": "Description of env var",
      "{{ENV_VAR_2}}": "Another env var"
    }
  },
  "dynamic": {
    "_comment": "These fields are refreshed automatically on every scan.",
    "last_scanned": "{{TIMESTAMP}}",
    "module_layout": {
      "{{SOURCE_DIR_1}}/": "Source code",
      "{{TEST_DIRECTORY}}/": "Test code"
    },
    "domain_concepts": [
      "{{DOMAIN_CONCEPT_1}}",
      "{{DOMAIN_CONCEPT_2}}"
    ],
    "agent_capabilities": [
      "code_analysis",
      "testing",
      "building"
    ],
    "test_command": "{{TEST_COMMAND}}",
    "build_command": "{{BUILD_COMMAND}}",
    "lint_command": ""
  }
}
