name: AgentReady - PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    # Secrets are unavailable on fork PRs — skip rather than silently no-op.
    # The installed template (pr-review-workflow.yml) uses pull_request_target
    # for target repos, which safely gives secret access from the base branch.
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -e '.[ai]' -q

      - name: Review PR
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          python src/agent_ready/cli.py \
            --target . \
            --review-pr "$PR_NUMBER" \
            --quiet
        continue-on-error: true
