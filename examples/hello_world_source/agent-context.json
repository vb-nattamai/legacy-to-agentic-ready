{
  "static": {
    "project_name": "hello_world",
    "description": "A minimal Flask REST API that provides personalised greetings, records them in memory, and exposes health-check and listing endpoints.",
    "primary_language": "Python",
    "runtime_version": ">=3.11",
    "frameworks": [
      "Flask",
      "pytest"
    ],
    "entry_point": "app.py",
    "test_command": "pytest",
    "restricted_write_paths": [],
    "environment_variables": [],
    "domain_concepts": [
      "Greeting: A recorded personalised message containing a user's name and a hello message, stored in the in-memory list.",
      "Health Check: A lightweight endpoint (/health) that returns an 'ok' status to confirm the service is running.",
      "Service Root: The index endpoint (/) that returns the service name and version metadata."
    ]
  },
  "dynamic": {
    "last_scanned": "2026-04-29T12:11:06.468272+00:00",
    "secondary_languages": [],
    "build_system": "pip",
    "build_command": "pip install -e '.[dev]' 2>/dev/null || pip install -r requirements.txt",
    "install_command": "pip install -r requirements.txt",
    "run_command": "python app.py",
    "test_framework": "pytest",
    "test_directory": "tests",
    "source_directories": [
      "."
    ],
    "module_layout": {
      "app": "app.py",
      "tests": "tests/"
    },
    "architecture_summary": "Single-module Flask application exposing four REST endpoints (index, health, greet, greetings). All state is held in an in-memory list within the app module\u2014there is no database or external dependency. The test suite exercises these endpoints through Flask's test client.",
    "key_components": [
      {
        "name": "app",
        "path": "app.py",
        "responsibility": "Defines the Flask application, all REST endpoints, and the in-memory greetings store."
      },
      {
        "name": "test_app",
        "path": "tests/test_app.py",
        "responsibility": "Contains the test suite that validates all API endpoints."
      }
    ],
    "agent_safe_operations": [
      "Adding new GET/POST endpoints to app.py",
      "Adding new test functions in tests/test_app.py",
      "Updating greeting message formatting",
      "Adding new utility modules alongside app.py",
      "Updating requirements.txt with new dependencies"
    ],
    "agent_forbidden_operations": [
      "Removing or renaming existing public endpoints without updating tests",
      "Changing the _greetings in-memory data structure without updating all consumers",
      "Changing the Flask app variable name 'app' (likely imported by tests)",
      "Removing pytest configuration from pyproject.toml"
    ],
    "potential_pitfalls": [
      "The _greetings list is module-level mutable state; tests that call /greet/<name> will accumulate greetings across test functions unless the test client or app is re-created per test.",
      "app.py serves as both the module defining the Flask app and the runnable entry point; importing it at module level triggers route registration and list initialisation.",
      "No database is used\u2014data is lost on restart and is not thread-safe under concurrent requests.",
      "The project has no extras_require 'dev' section in pyproject.toml, so 'pip install -e .[dev]' in the Makefile will fall back to requirements.txt.",
      "Flask test client must be obtained from the 'app' object in app.py; renaming that variable will break tests."
    ],
    "conventions": {
      "naming": "snake_case",
      "structure": "single-package"
    },
    "has_ci": true,
    "has_openapi": false
  }
}