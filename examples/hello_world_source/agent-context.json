{
  "static": {
    "project_name": "hello_world",
    "description": "A minimal Flask REST API that provides personalised greetings, records them in memory, and exposes health-check and listing endpoints.",
    "primary_language": "Python",
    "runtime_version": ">=3.11",
    "frameworks": [
      "Flask",
      "pytest"
    ],
    "entry_point": "app.py",
    "test_command": "pytest",
    "restricted_write_paths": [],
    "environment_variables": [],
    "domain_concepts": [
      "Greeting: A recorded personalised message containing a user's name and a hello message, stored in the in-memory list.",
      "Health Check: A lightweight endpoint (/health) that returns an 'ok' status to confirm the service is running.",
      "Service Root: The index endpoint (/) that returns the service name and version metadata."
    ]
  },
  "dynamic": {
    "last_scanned": "2026-04-29T11:38:48.004268+00:00",
    "secondary_languages": [],
    "build_system": "pip",
    "build_command": "pip install -e '.[dev]' 2>/dev/null || pip install -r requirements.txt",
    "install_command": "pip install -r requirements.txt",
    "run_command": "python app.py",
    "test_framework": "pytest",
    "test_directory": "tests",
    "source_directories": [
      "."
    ],
    "module_layout": {
      "app": "app.py",
      "tests": "tests/"
    },
    "architecture_summary": "A single-file Flask application that exposes four REST endpoints (index, health, greet, and greetings). All state is held in an in-memory list; there is no database or external dependency. Tests live in a dedicated tests/ directory and exercise the API through Flask's test client.",
    "key_components": [
      {
        "name": "app",
        "path": "app.py",
        "responsibility": "Defines the Flask application, all route handlers, and the in-memory greetings store."
      },
      {
        "name": "test_app",
        "path": "tests/test_app.py",
        "responsibility": "Contains tests for the API endpoints."
      }
    ],
    "agent_safe_operations": [
      "Adding new GET/POST endpoints in app.py",
      "Adding new test files or test functions under tests/",
      "Updating dependencies in requirements.txt and pyproject.toml",
      "Refactoring greeting logic within app.py",
      "Running pytest to validate changes"
    ],
    "agent_forbidden_operations": [
      "Removing or renaming existing public endpoints (/, /health, /greet/<name>, /greetings) without updating tests",
      "Changing the _greetings in-memory store to a database without explicit instruction",
      "Modifying cost_report.json",
      "Changing the default port (5000) or host binding without explicit instruction"
    ],
    "potential_pitfalls": [
      "The _greetings list is module-level global state; it persists across requests in a single process but resets on restart, and tests that don't isolate state will see cross-test pollution.",
      "Flask's test client must be obtained via app.test_client(); importing app directly and using httpx against localhost requires the server to actually be running.",
      "All routes use @app.get() shorthand (Flask 2.x+); using @app.route() with methods=['GET'] is equivalent but mixing styles can confuse linters.",
      "There is no CORS, authentication, or input validation \u2014 adding middleware must be tested carefully to avoid breaking existing endpoint contracts.",
      "pyproject.toml declares setuptools build backend but there is no setup.cfg or package directory, so 'pip install -e .' may fail without the requirements.txt fallback."
    ],
    "conventions": {
      "naming": "snake_case",
      "structure": "single-package"
    },
    "has_ci": true,
    "has_openapi": false
  }
}