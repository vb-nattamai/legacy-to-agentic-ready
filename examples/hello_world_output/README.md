# hello_world — AgentReady example output

This directory contains the complete set of files generated by AgentReady against a minimal Flask API project.

## Source project

A single-file Python/Flask REST API with three endpoints (`/`, `/health`, `/greet/<name>`), a pytest test suite, and a GitHub Actions CI workflow.

## What was generated

| File | Generator |
|---|---|
| `agent-context.json` | LLM (analysis + generation) |
| `AGENTS.md` | LLM |
| `CLAUDE.md` | LLM |
| `.cursorrules` | Deterministic (from analysis) |
| `.github/copilot-instructions.md` | LLM |
| `system_prompt.md` | LLM |
| `mcp.json` | Deterministic |
| `memory/schema.md` | LLM |
| `skills/run-tests.md` | LLM (pytest detected) |
| `skills/build.md` | LLM (always generated) |
| `skills/run-ci.md` | LLM (CI config detected) |
| `skills/add-dependency.md` | LLM (pip detected) |
| `hooks/session-start.md` | LLM (always generated) |
| `hooks/pre-tool-call.md` | LLM (always generated) |
| `hooks/post-test.md` | LLM (test runner detected) |
| `tools/refresh_context.py` | Deterministic |
| `.github/dependabot.yml` | Deterministic |
| `.github/CODEOWNERS` | Deterministic |
| `.agent-ready/custom_questions.json` | Deterministic |
| `openapi.yaml` | LLM (Flask REST API detected) |

**Agentic Readiness Score: 85 / 100**

## Command used

```bash
agent-ready --target /path/to/hello_world --provider anthropic --force
```

## How to use this as a reference

These files show what AgentReady produces on a well-structured minimal Python project. Use them to:

- Understand the format and content of each generated artifact
- Calibrate expectations before running on your own repository
- Check that the skills and hooks sections are correctly populated for a pytest + pip + CI project
